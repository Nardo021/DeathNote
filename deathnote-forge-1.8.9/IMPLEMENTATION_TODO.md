# Implementation status — Forge 1.8.9

**DONE.** Full mod implemented (independent Gradle 4.9 + ForgeGradle 2.1, Java 8).

```bash
cd deathnote-forge-1.8.9
./gradlew build   # JAVA_HOME must be JDK 8
```

Output: `build/libs/deathnote-forge-1.8.9-1.0.0-SNAPSHOT.jar`
