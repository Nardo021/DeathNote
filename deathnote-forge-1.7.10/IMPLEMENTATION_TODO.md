# Implementation status — Forge 1.7.10

**DONE.** Full mod implemented (independent Gradle 4.10.3 + ForgeGradle 1.2, Java 8).

```bash
cd deathnote-forge-1.7.10
./gradlew build   # JAVA_HOME must be JDK 8
```

Output: `build/libs/deathnote-forge-1.7.10-1.0.0-SNAPSHOT.jar`
